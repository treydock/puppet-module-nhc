fixtures:
  forge_modules:
    stdlib:
      repo: puppetlabs/stdlib
    yumrepo_core:
      repo: puppetlabs/yumrepo_core
    vcsrepo:
      repo: puppetlabs/vcsrepo
    logrotate:
      repo: puppet/logrotate
    yum:
      repo: puppet/yum
  symlinks:
    nhc: "#{source_dir}"
